import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hcm.snapchat',
  appName: 'Snapchat HCM',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
